# Install from the project folder (steve_cabs) with your virtual environment active:
#   pip install -r requirements.txt
# Python 3.10+ recommended.

Django==4.2.7
channels==4.0.0
daphne==4.0.0
channels-redis==4.2.0

razorpay==1.4.2
# Pillow==10.1.
Pillow>=11.0.0
whitenoise>=6.0.0

google-generativeai==0.3.2
ollama==0.6.1
httpx>=0.24.0
httpcore>=0.24.0
requests>=2.28.0

# Translation & text-to-speech (tickets, recordings)
deep-translator>=1.11.4
gTTS==2.5.1
edge-tts>=6.1.0
pyttsx3>=2.90

# Trip recording: local Whisper transcription (downloads model on first use)
# numpy>=1.24.0,<2.1
numpy>=2.0.0
soundfile>=0.12.0
faster-whisper>=1.0.0

# Optional heavy deps (utility script steve_cabs/generate_voice.py). If install is slow or fails on Windows,
# install CPU PyTorch first, then re-run: pip install -r requirements.txt
#   pip install torch --index-url https://download.pytorch.org/whl/cpu
torch>=2.0.0
transformers>=4.30.0
